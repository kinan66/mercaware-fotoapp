import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Mercaware - Fotoapp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
